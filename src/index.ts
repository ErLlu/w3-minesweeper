import { Board } from "./types.js";

const board: Board = {
  totalMines: 0,
  numberOfRows: 4,
  numberOfColumns: 4,
  Squares: [],
};

export const createBoard = (
  numberOfRows: number,
  numberOfColums: number,
): Board => {};
